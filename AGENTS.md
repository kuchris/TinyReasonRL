# Change Workflow

- Create a separate Git commit for each logically distinct change so it can be reviewed and traced independently.
- After making a change, run the relevant tests and update any affected tests, documentation, configuration, or generated files.
- Do not consider the task complete until all relevant checks pass and there is clear evidence that the change works.
- Present the completed, verified result to the user, including the commits created and the checks performed.
